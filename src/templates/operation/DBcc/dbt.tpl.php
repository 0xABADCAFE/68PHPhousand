<?php

/**
 * DBT
 *
 */

assert(!empty($oParams), new \LogicException());

// Surely this instruction is invalid?
?>
return function(int $iOpcode): void {
    $this->iProgramCounter = ($this->iProgramCounter + ISize::WORD) & ISize::MASK_LONG;
};

